# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment and secrets
.env
.env.*
!.env.example

# Supabase temp
supabase/.temp/
.vercel

# Local Netlify folder
.netlify

# Personal Obsidian vaults / notes (not project source)
/Obsidian/
/Pharmasync Vault/

# Scratch/diagnostic scripts
*.tmp.mjs

# Real-patient-data seed migrations (generated by scripts/import_kempas_quota.py
# and similar). These contain actual names/IC numbers and must never be
# committed to git — deliver to the target DB directly, never via git/GitHub.
supabase/migrations/*_seed_*.sql

# superpowers subagent-driven-development scratch (per-plan workspaces)
.superpowers/
